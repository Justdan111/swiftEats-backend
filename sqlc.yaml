version: "2"

sql:
  - schema: "internal/migrations"
    queries: "internal/db/queries"
    engine: "postgresql"
    gen:
      go:
        out: "internal/db"
        package: "db"
        emit_json_tags: true
        emit_prepared_queries: true
